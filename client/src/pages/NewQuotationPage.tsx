// NewQuotationPage — thin wrapper around the shared QuotationForm
import QuotationForm from '@/components/quotation/QuotationForm';

export default function NewQuotationPage() {
    return <QuotationForm />;
}
